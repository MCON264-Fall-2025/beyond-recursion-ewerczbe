Observations:
While running the recursive vs the iterative fibonacci sequence, i noticed the time being way longer by the 
recursive at O(2^n) and the iterative time stays linear and smaller no matter if the number in the function is higher...
I noticed the Same thing by the insertLast recursive vs the insertLast iterative approach, a stack overflow occurred by 
the recursive when the list was 50000 and when i lowered the lists for both the recursive time was still largely longer 
than the iterative approach. and the iterative time complexity stayed linear even at 50000